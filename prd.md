
---

# 1. **Цель продукта**

Создать инструмент командной строки (CLI), который:

* принимает на вход **директорию с Markdown-файлами**;
* вычисляет на основе директории с файлами соответствующую директорию с изображениями для документов из ссылок в маркдаун и на основе иерархии 
файлов маркдаун в текущей папке;
* автоматически **собирает** все Markdown-файлы в единую структуру;
* автоматически **собирает** все картинки для файлов;
* применяет **стили форматирования и оформление согласно ГОСТ-подобному стайлгайду**;
* использует **Pandoc + LaTeX** как движок рендеринга PDF;
* выдаёт **итоговый PDF-документ высокой типографской точности**.

Продукт должен обеспечить единый стандартизованный процесс генерации формализованных документов любого объёма — от 10 до 400 страниц.

---

# 2. **Основные сценарии использования**

### **2.1. Сценарий 1 — Разработчик документации**

* Пишет множество Markdown-файлов (главы, разделы, приложения).
* Хранит картинки в папке с тем же уровнем вложенности, но без цифровых префиксов в названиях папок.
* Запускает CLI-команду и получает PDF по ГОСТ-стилю.


---

# 3. **Функциональные требования**

## 3.1. Вводные данные

Конвертер должен принимать:

* `--md-dir` — главная папка с Markdown-файлами (корень дерева вида `content/003.cu`).
* `--config` — файл конфигурации проекта (структура документа, метаданные; опционален, если сборка идёт строго по иерархии).
* `--style` — стиль оформления (имя файла в `styles/`, например `style` → `styles/style.yaml`).
* `output.pdf` — путь к итоговому файлу.

Пример вызова:

```bash
md2pdf \
  --md-dir ./docs \
  --config ./config/project.yml \
  --style gost \
  ./output/report.pdf
```

---

## 3.2. Обработка Markdown-файлов

Конвертер должен:

1. Найти все файлы `.md` в дереве `content/<NNN.slug>/`.
2. Определить порядок включения строго по иерархии из `hierarchy-analysis.md`:

   * старт с `content/<NNN.slug>/0.index.md`;
   * на каждом уровне сначала `0.index.md` (если есть), затем остальные `.md` по числовому префиксу;
   * для директорий — рекурсия в числовом порядке; `content/003.cu/doc/*` пропускается как без контента;
   * если `project.yml` задан, он может переопределять порядок, но базовая рекурсия — источник правды.
3. Считать содержимое каждого файла.
4. Обрабатывать ссылки на изображения, используя отображение Markdown → `/images/<doc-slug>/...`:

   * полный путь: `<doc-slug>` — имя папки без числового префикса (`003.cu` → `cu`);
   * далее — сегменты пути с удалёнными числовыми префиксами и точками (`02.ekspluatatsiya/060000.ctrl-uzlam` → `ekspluatatsiya/ctrl-uzlam`);
   * обычные `<img src="/images/.../imageNN.png" data-resource-id="imageNN" />` валидируются как есть;
   * блоки `::sign-image` с `src: /imageNN.png` резолвятся в вычисленную папку картинки для текущего markdown.
5. Учитывать front matter: YAML в начале файла; поддержка минимум для ключей `title`, `draft`, `chapter-title` (игнорировать неизвестные безопасно).

---

## 3.3. Сборка единого документа (bundle.md)

Программа должна:

* Склеить файлы в единый `bundle.md`.
* Вставлять разделители страниц (`\newpage` или аналог Pandoc).
* Вставлять YAML-межу (metadata) в начало документа.
* Создать структуру документа:

  * титульный лист,
  * оглавление,
  * главы,
  * приложения.

---

## 3.4. Применение стиля

Стиль определяется:

* файлом `styles/<style>.yaml` (ключи — токены, которые маппятся на переменные Pandoc/LaTeX в `templates/<style>.tex`),
* шаблоном `templates/<style>.tex`.

Стиль должен задавать:

* шрифты (основной/моно, размеры заголовков и текста),
* межстрочные интервалы,
* поля страницы,
* оформление заголовков,
* нумерацию страниц,
* колонтитулы,
* титульный лист.

Пример ключей в `styles/style.yaml` (стиль «ROSA ГОСТ»):

```yaml
document:
  class: article
fonts:
  main: "Roboto"
  mono: "Roboto Mono"
  base_size: 11pt
page:
  size: a4paper
  margins:
    left: 30mm
    right: 15mm
```

---

## 3.5. Рендеринг PDF

### Требования:

* Использовать **Pandoc** как движок конвертации.
* Использовать **LaTeX (xelatex или lualatex)** как PDF-рендер.
* Поддерживать:

  * многостраничность,
  * таблицы любой ширины,
  * изображения,
  * подписи к рисункам,
  * списки,
  * нумерацию разделов.

Пример команды:

```bash
pandoc bundle.md \
  --from markdown+yaml_metadata_block \
  --template=templates/gost.tex \
  --pdf-engine=xelatex \
  --toc \
  -o output/report.pdf
```

---

## 3.6. Lua-фильтры (по необходимости)

Поддержка Lua-фильтров для:

* оформления подписей «Рисунок X — …»,
* преобразования специальных блоков (`:::note`),
* нумерации приложений,
* переноса Markdown-фрагментов под требования ГОСТ.

Фильтры подключаются через `project.yml`:

```yaml
filters:
  - filters/gost_figures.lua
  - filters/notes.lua
```

---

# 4. **Нефункциональные требования**

## 4.1. Производительность

* Документ до 400 страниц (примерно 300–350 markdown-файлов, как у `content/003.cu`) должен собираться < 8 секунд (локально).
* Ограничение: LaTeX занимает 2–4 секунды на рендеринг; остальное время — на обход/склейку/валидацию картинок.

## 4.2. Надежность

* Ошибки Pandoc или LaTeX должны быть перехвачены и выданы в читабельном виде.
* Конвертер не должен падать даже при неправильных ссылках.

## 4.3. Портируемость

* Работает в Linux (Ubuntu 22/24).

## 4.4. Расширяемость

* Можно добавлять новые стили (`style.yaml + template.tex`).
* Можно добавлять новые фильтры.
* Можно добавлять новые схемы фронтматтера.

---

# 5. **Технологический стек**

### 🟦 Основа

* **Python 3.11+** — логика, CLI, сборка файлов, конфиги.
* **Pandoc 3.x** — конвертация Markdown → LaTeX.
* **LaTeX (TeXLive) + xelatex** — PDF-рендеринг.
* **YAML** — формат конфигураций.
* **Lua** — дополнительные фильтры (опционально).

### 🟧 Структура приложения (ожидаемая)

```
md2pdf/
  src/
    cli.py
    bundler.py
    config_loader.py
    file_collector.py
    metadata.py
    pandoc_runner.py
    filters/
  templates/
    gost.tex
  styles/
    style.yaml
```

---

# 6. **Пайплайн работы конвертера**

1. **CLI получает параметры**
2. **Загружается `project.yml` и `style.yaml`**
3. **Сканируются Markdown-файлы**
4. **Определяется путь к картинкам**
5. **Определяется порядок глав**
6. **Создаётся `bundle.md`**
7. **Добавляется YAML-межа с метаданными**
8. **Вызывается Pandoc с нужным шаблоном**
9. **LaTeX генерирует PDF**
10. **Программа возвращает путь к PDF**

---

# 7. **Пользовательский интерфейс (CLI)**

Пример команды:

```bash
md2pdf \
  --md-dir ./docs \
  --config ./config/project.yml \
  --style gost \
  ./output/report.pdf
```

CLI должен:

* показывать прогресс,
* валидировать входные данные,
* выводить ошибки Pandoc/LaTeX в удобной форме.

---

# 8. **Требования к ГОСТ-стилю**

* Шрифт по умолчанию — Roboto 11 пт (основной) и Roboto Mono для кода; допускается замена стилем, но шаблон `gost.tex` ожидает явные значения `mainfont`/`monofont`/`fontsize`.
* Межстрочный интервал: 1.15 (может быть переопределён стилем).
* Поля: слева 30 мм, справа 15 мм, сверху/снизу — 20 мм.
* Выравнивание основного текста: по ширине с переносами.
* Нумерация рисунков: «Рисунок N – Название».
* Нумерация таблиц: «Таблица N – Название».
* Титульный лист: согласно шаблону `templates/gost.tex` (логотип, организация, продукт, тип документа).
* Валидация: стиль должен обеспечивать совместимость своих ключей с переменными, используемыми в шаблоне (например, вычислять `geometry`, `lineheight`, размеры для заголовков).

---

# 9. **Ограничения**

* Pandoc должен быть версии 3.x.
* Стили должны быть заранее подготовлены.

---

# 10. **Критерии готовности (Definition of Done)**

Текущий прогресс:
- Базовый обход Markdown реализован (`walker`) с предупреждениями о проблемах структуры.
- Подготовлены шаблон и стиль (`templates/gost.tex`, `styles/style.yaml`).
- Остальные пункты в работе.

Чек-лист:
* [ ] Конвертер работает через CLI.
* [ ] Читает `project.yml` и `style.yaml`.
* [ ] Поддерживает вложенные папки Markdown (базовый обход есть, интеграция в CLI/бандлер впереди).
* [ ] Собирает единый PDF.
* [ ] PDF соответствует ГОСТ-стилю.
* [ ] Поддерживает изображения.
* [ ] Ошибки Pandoc/LaTeX показываются корректно.
* [ ] Можно подключить новый стиль без изменения кода.
* [ ] Есть README.

---
